# Red 1 assembler 
Just a helper I wrote in like 30 seconds to turn my assembly into machine code. Will be making a better version in the next iteration. 

To use the assembler, write your program inside the programs/asm directory then run this command: 

```
node index.js path_to_asm.txt
```

It will then write to a file in the programs/raw directory. 